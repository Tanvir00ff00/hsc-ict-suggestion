import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tanvir.hscict2026',
  appName: 'HSC ICT 2026',
  webDir: 'dist/public',
  server: {
    androidScheme: 'https'
  }
};

export default config;
